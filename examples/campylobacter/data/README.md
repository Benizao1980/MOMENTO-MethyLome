# Example data

The toy methylation table is synthetic and exists only to demonstrate MOMENTO workflows. Real/private data should stay outside the public Git repository and be transformed to the same canonical schema.
